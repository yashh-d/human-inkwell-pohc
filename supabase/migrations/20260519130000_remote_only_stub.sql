-- Stub for a migration that was applied directly to the hosted Supabase project
-- via the dashboard (no SQL recorded in this repo). Kept here only so the
-- Supabase CLI migration history matches between local and remote and
-- `supabase db push` doesn't refuse to run.
--
-- No-op — the actual schema change already exists on the remote.
select 1;
